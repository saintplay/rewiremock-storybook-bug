Minimal Repository to debug rewiremock and storybook bug

# Test File

Using rewiremock in `/src/stories/index.js/`

# Notes

adding

```js
rewiremock.overrideEntryPoint(module);
```

changes the error
